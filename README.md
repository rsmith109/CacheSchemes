# CacheSchemes
This project is representing how a cache in a cpu would work to save time from talking to memory\
The cache uses many different schemes represented by different data structures and benchmarks them\
FIF (Furthest in future) is an unrealistic search method that knows the next needed information used as a benchmark test\
All other schemes are implemented and tested to see how they preform against each other
